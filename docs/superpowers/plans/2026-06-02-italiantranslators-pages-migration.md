# Italian Translators Pages Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the current GitHub Pages repository into the source repository for the Italian Translators Vue site, with automated GitHub Pages deployment and a stable `/italiantranslators/` base path.

**Architecture:** Replace the hand-checked static `italiantranslators/` output with the original Vite/Vue source tree, keep production assets generated by CI instead of storing `dist`, and publish through GitHub Pages Actions. The repository itself will be renamed on GitHub to `italiantranslators` so the project site URL becomes `https://cudrx.github.io/italiantranslators/`.

**Tech Stack:** Vue 3, Vite 5, Sass, GitHub Pages, GitHub Actions

---

### Task 1: Add Missing Build Dependency

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`

- [ ] Add `sass` to `devDependencies`.
- [ ] Verify the lockfile also includes `sass`.

### Task 2: Convert Repo To Source-Based Layout

**Files:**
- Create: `.gitignore`
- Create: `.prettierrc`
- Create: `README.md`
- Create: `index.html`
- Create: `jsconfig.json`
- Create: `vite.config.js`
- Create: `public/favicon.ico`
- Create: `src/main.js`
- Create: `src/App.vue`
- Create: `src/assets/main.scss`
- Create: `src/assets/data.js`
- Create: `src/assets/regions/*.js`
- Delete: `italiantranslators/index.html`
- Delete: `italiantranslators/assets/*`
- Delete: `italiantranslators/favicon.ico`

- [ ] Copy the Vite source files from `vue-translators` into this repository.
- [ ] Remove the old committed static output folder so deployment comes from CI build artifacts instead.

### Task 3: Add GitHub Pages Deployment Workflow

**Files:**
- Create: `.github/workflows/deploy-pages.yml`

- [ ] Add a workflow that installs dependencies with `npm ci`, builds with `npm run build`, uploads `dist`, and deploys to GitHub Pages on pushes to `main`.

### Task 4: Verify Build End-To-End

**Files:**
- Test: local repository root

- [ ] Run `npm ci`.
- [ ] Run `npm run build`.
- [ ] Confirm the generated `dist/index.html` references `/italiantranslators/` asset paths.

### Task 5: Publish And Rename

**Files:**
- Modify: Git history only

- [ ] Commit the migration.
- [ ] Push `main` to GitHub.
- [ ] Attempt to rename the GitHub repository from `artyom-tugushev.github.io` to `italiantranslators`.
- [ ] If the rename must be completed outside the current toolset, report the exact manual step still required.
